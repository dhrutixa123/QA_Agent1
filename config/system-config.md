Mock Mode: OFF

If Mock Mode is ON:
- Do NOT connect to Azure DevOps
- Do NOT require Azure credentials
- Use a local mock ticket instead
- Use a public demo website for browser testing

If Mock Mode is OFF:
- Use Azure DevOps API
- Use real application URL
- Use real credentials
